name: marvel_app
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^0.13.4
  dartz: ^0.10.1
  crypto: ^3.0.1
  provider: ^6.0.2
  equatable: ^2.0.3
  cupertino_icons: ^1.0.2
  google_fonts: ^2.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  mocktail: ^0.3.0
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  assets:
  - assets/icons/
  - assets/images/